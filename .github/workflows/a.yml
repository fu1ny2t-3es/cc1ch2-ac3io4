on:
  workflow_dispatch:
  schedule:
    - cron: '45 1 */6 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/cc1ch2-ac3io4@cron
        with:
          token: ${{ secrets.token }}
